LOS MAPS SE UTILIZAN CUANDO TIENES UNA CLAVE Y UN VALOR 

	MAP<K(tiene que ser una clase), V> MAPA = NEW HASHMAP<>();
	
	MAPA.PUT PARA INSERTAR (SE HAN DE INSERTAR TANTO LA CLAVE K COMO LA CLASE V )
	
	MAPA.GET(K); = NOS DEVUELVE LA CLASE V SI LE DAMOS LA CLAVE.

	MAPA.CONTAINSKEY(K);

//PARA OBTENER LAS CLAVES DEL MAPA ESCRIBIMOS//

	SET<K> CLAVES = MAPAS.KEYSET();
	SOUT(CLAVES);
	CLAVES.REMOVE("EJEMPLO");
	
//ORDENACION//

	COLLECTION<V> ORDENACIONPREDEFINIDA = MAPA.VALUES();
	
//MAP.ENTRY

	SIRVE PARA OBTENER TODAS LAS ENTRADAS, TANTO LA CLAVE COMO LA CLASE QUEDANDO DE LA SIGUIENTE FORMA

	List<Map.Entry<Character, Academico>> listaConLetra = new ArrayList<>(conLetra);